import React from 'react';

const currentYear = new Date().getFullYear();
function Footer(){
    return(
        <footer>
            <h2>Get in touch</h2>
        </footer>
    );
};

export default Footer;